---
import DashboardLayout from "@/layouts/DashboardLayout.astro";
---

<DashboardLayout title="Welcome" description="Git Folders Welcome Page">
  <div
    class="flex min-h-screen cursor-default items-center justify-center bg-gradient-to-t from-slate-950 to-black px-4 py-6 md:px-16 md:py-20"
  >
    <div
      class="container mx-auto w-full max-w-3xl space-y-6 rounded-xl border border-slate-700 bg-gradient-to-b from-gray-950 to-zinc-950 p-6 shadow-lg shadow-slate-950"
    >
      <div class="flex flex-col gap-y-4">
        <h1 class="text-center text-4xl font-bold text-slate-100 md:text-6xl">
          Welcome to GitFolders!
        </h1>
        <p class="text-center text-lg font-medium text-slate-300">
          Your GitHub just got an upgrade. Organize your repositories
          effortlessly.
        </p>
      </div>
      <div
        class="container mx-auto max-w-md rounded-xl border border-slate-700 bg-gradient-to-b from-slate-900 to-zinc-950 px-6 py-4"
      >
        <p class="mb-4 font-medium text-slate-100">
          Your account is set up! Here's what you can do next:
        </p>
        <ul class="mb-6 list-inside list-disc text-left text-slate-300">
          <li>Create and manage custom repository folders</li>
          <li>Sync seamlessly between local and cloud storage</li>
          <li>Access your organized GitHub from anywhere</li>
          <li>Enjoy a clean, distraction-free GitHub experience</li>
        </ul>
        <!-- TODO: Delete this after beta programme -->
        <div class="flex flex-col items-center gap-1">
          <a
            href={"https://github.com/"}
            class="inline-block rounded-lg bg-blue-600 px-6 py-3 font-medium text-slate-100 transition-colors hover:bg-blue-800"
          >
            Go to Github</a
          >
          <p class="text-sm text-slate-400">Say Hello to new Github.</p>
        </div>
        <!-- TODO: Uncomment this after beta programme -->
        <!-- <div class="flex flex-col items-center gap-1">
          <a
            href="#"
            id="subscribe-button"
            class="inline-block rounded-lg bg-blue-600 px-6 py-3 font-medium text-slate-100 transition-colors hover:bg-blue-800"
          >
            Subscribe Now
          </a>

          <script>
            import { supabase } from "@lib/supabase";
            import { PAYMENT_LINK } from "@/NavRoutes";

            const getUserID = async () => {
              const {
                data: { user },
              } = await supabase.auth.getUser();
              if (!user) return;
              const paymentLink = new URL(PAYMENT_LINK);
              paymentLink.searchParams.set("userId", user.id ?? "");
              const subscribeBtn = document.getElementById("subscribe-button");
              if (subscribeBtn) {
                (subscribeBtn as HTMLAnchorElement).href =
                  paymentLink.toString();
              }
            };

            // Run check when page loads
            getUserID();
          </script>
          <p class="text-sm text-slate-400">Subscribe to continue.</p>
        </div> -->
      </div>
    </div>
  </div>
</DashboardLayout>
