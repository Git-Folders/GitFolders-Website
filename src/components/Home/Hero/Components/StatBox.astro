---
export type Stat = {
  value: string;
  label: string;
};
const { value, label } = Astro.props as Stat;
---

<div class="text-center">
  <div class="text-accent text-3xl font-bold">{value}</div>
  <div class="text-text-secondary text-sm font-medium">{label}</div>
</div>
